# 运行说明

## ✅ 已完成的步骤

1. ✅ 代码已修复，支持 Java 8
2. ✅ 编译测试通过
3. ✅ 数据生成测试通过（20MB数据，约3.5秒）
4. ✅ 数据合并测试通过（约0.15秒）

## 🚀 需要您运行的步骤

由于执行Flink作业可能需要较长时间（可能超过10秒），请您运行以下命令：

### 方式1: 运行完整脚本（推荐）

```bash
cd /Users/ron/Documents/Code/self/IP/cquirrel/new
./run.sh
```

脚本会自动：
1. 设置Python环境（.venv）
2. 生成数据（20MB，约3-4秒）
3. 合并数据（约0.15秒）
4. 编译项目（约5秒）
5. **执行Flink作业**（需要您等待，会有进度提示）
6. 验证结果（使用.venv中的Python）

### 方式2: 分步运行

如果数据已生成，可以直接执行：

```bash
cd /Users/ron/Documents/Code/self/IP/cquirrel/new

# 执行Flink作业
echo "开始执行Flink作业..."
java -jar target/stream-query-engine-1.0.0.jar > output.txt 2>&1

# 验证结果
cd scripts
source ../.venv/bin/activate
python result_validator.py > ../validation.txt 2>&1
cd ..
```

## 📊 预期输出

运行完成后会生成：
- `scripts/data/output.csv`: 处理结果
- `output.txt`: 执行日志
- `validation.txt`: 验证结果

## ⚠️ 注意事项

1. **Java版本**: 确保使用 Java 8 或更高版本
2. **内存**: 如果遇到内存不足，可以增加JVM内存：
   ```bash
   java -Xmx2g -jar target/stream-query-engine-1.0.0.jar
   ```
3. **进度**: 脚本会实时打印进度，执行Flink作业时请耐心等待

## 🔍 检查结果

运行完成后，检查验证结果：

```bash
tail -20 validation.txt
```

如果看到 "✅ Results are identical"，说明结果正确！

